<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
        
<hibernate-configuration>
	
	<session-factory>
		<!-- This property specifies our Driver class. -->
		<property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
		<!-- Specify the dialect of SQL that we're using -->
		<property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>
		<!-- Specify your database credentials. Note that you shouldn't actually hardcore your credentials into this file. -->
		<property name="hibernate.connection.url"></property>
		<property name="hibernate.connection.username"></property>
		<property name="hibernate.connection.password"></property>
		<!-- This property allows us to see the SQL queries generated by Hibernate in the console -->
		<property name="show_sql">true</property>
		<!-- This property determines how many connections a single user is allowed to your DB. -->
		<property name="hibernate.connection.pool_size">5</property>
		<!-- This property defines how Hibernate sesssions are handled. In this case, sessions are tracked by their thread of execution -->
		<property name="hibernate.current_session_context_class">thread</property>
		<!-- Will use the validate mode because the schema already exists -->
		<property name="hibernate.hbm2ddl.auto">validate</property>
		<!-- Need to tell Hibernate where the mapped resources are; it doesn't auto detect the resources. -->
		<mapping class="com.revature.model.Users"/>
		<mapping class="com.revature.model.Flight"/>
		<mapping class="com.revature.model.UserFlight"/>
	</session-factory>

</hibernate-configuration>